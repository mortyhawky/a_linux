###########################################
##  PREFIX = Alt+Space
############################################
unbind-key C-b
unbind-key Space
set-option -g prefix M-Space
bind-key M-Space send-prefix

######################################################
##	SOURCE
######################################################
# Ctrl + Alt + r
bind-key -Troot M-C-r source-file ~/.config/tmux/tmux.conf \
                    ; display-message "Sourced"
# Alt + r
bind-key -Troot M-r   source-file ~/.config/tmux/tmux.conf \
                    ; display-message "Sourced"
# Prefix + r
bind-key r            source-file ~/.config/tmux/tmux.conf \
                    ; display-message "Sourced"

############################################
##  GLOBAL OPTIONS
############################################
# Add the following to your tmux configuration file, 
# replacing XXX by the value of $TERM outside of tmux:
set-option -a terminal-features 'linux:RGB'
set-option -g base-index 1
set-option -g pane-base-index 1
set-option -g focus-events on
set-option -g mode-keys vi

######################################################
##	NAVIGATION
######################################################
bind-key -Troot M-| split-window -h
bind-key -Troot M-` split-window -h 	\
                  ; display-message "Alt+| split V"
bind-key -Troot M-- split-window -v
bind-key -Troot M-/ split-window -v     \
                  ; display-message "Alt+- split H" 

bind-key -Troot M-h select-pane -L
bind-key -Troot M-j select-pane -D
bind-key -Troot M-k select-pane -U
bind-key -Troot M-l select-pane -R

bind-key -Troot M-i resize-pane -D 1
bind-key -Troot M-o resize-pane -U 1
bind-key -Troot M-u resize-pane -L 1
bind-key -Troot M-p resize-pane -R 1

bind-key -Troot M-H resize-pane -D 1
bind-key -Troot M-J resize-pane -U 1
bind-key -Troot M-K resize-pane -L 1
bind-key -Troot M-L resize-pane -R 1

bind-key -Troot M-n next-layout \
                  ; display-message "next-layout"

## Alt+|/-       For Linux tty console
#set-option -s user-keys[0] "\033[25~"
#set-option -s user-keys[1] "\033[26~"
#bind-key -n User0 split-window -h
#bind-key -n User1 split-window -v

# Define a custom user keys for the Linux console's F13&F14 sequences
# Needs that /etc/vconsole.conf has: KEYMAP=no-custom
# And that /usr/share/kbd/i386/qwerty/no-custom.map.gz has F13 and F14 mappings
#set-option -s user-keys[0] "\033[25~"
#set-option -s user-keys[1] "\033[26~"
#set-option -s user-keys[2] "\033[1;2P"
#set-option -s user-keys[3] "\033[1;2Q"
# Bind it
#bind-key -n User0 split-window -h
#bind-key -n User1 split-window -v
#bind-key -n User2 split-window -h
#bind-key -n User3 split-window -v

# test binding:
#bind-key -n User0 display-message "User0 pressed"

# Prefix+|/-    Works in non-gui and gui mode
bind-key      | split-window -h
bind-key      - split-window -v
############################################
##  COPY AND PASTE
############################################
# Alt+c     Enter Copy Mode
bind-key -n M-c copy-mode
# Alt+v:    Begin selection directly from normal mode
bind-key -n M-v copy-mode \; send-keys -X begin-selection
# when in copy-mode: v begin selection
bind-key -T copy-mode-vi v send-keys -X begin-selection
# when in copy-mode: y yank and exit copy-mode
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
# Escape to cancel copy mode
bind-key -T copy-mode-vi Escape send-keys -X cancel
# Alt+p     Put in normal mode
bind-key -n M-p paste-buffer

############################################
##  STATUS LINE
############################################
set-option -g status-style bg=blue
# Same blue color as the default DWM bar
#set -g status-bg "#005577"
set -g status-bg "#222222"
# Same white color as the default DWM foreground
set -g status-fg "#eeeeee"
set -g status-left  "#[bg=#000000,fg=#eeeeee] #S "
set -g status-right "#[bg=#000000,fg=#eeeeee] %Y-%m-%d %H:%M %a "

bind-key B set-option -g status-style bg=blue
bind-key C set-option -g status-style bg=cyan
bind-key R set-option -g status-style bg=red
bind-key F set-option -g status off
bind-key O set-option -g status on


#---------------------------------------------------------------------
############################################
## USER KEYS FROM TTY
############################################
#set -s user-keys[0] "\e[25~"           F13
#set -s user-keys[0] "\e[26~"|          F14
##set -s user-keys[0] "^[[25~"
##set -s user-keys[1] "^[[26~"
##set -s user-keys[0] "\e[1:2P"         F13
##set -s user-keys[1] "\e[1:2Q"         F14
#
#bind-key User0 split-window -h
#bind-key User1 split-window -v
#bind-key -n M-v split-window -h
#bind-key -n M-h split-window -v
